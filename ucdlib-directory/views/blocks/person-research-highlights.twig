{% set hide = attributes.hideResearchHighlights == 1 or attributes.hideResearchHighlights is same as(true) %}

{% if not hide %}
  <ucdlib-directory-research-highlights
    expert-id="{{ attributes.expertId }}"
    data-lazy="true"
    class="u-space-mb">
  </ucdlib-directory-research-highlights>
{% endif %}