{% if attributes.libraries and not attributes.hide %}
  <div class="u-space-bottom">
    <span class="icon icon--location">
    {% apply spaceless %}
      {% for library in attributes.libraries %}
        <span>{{library.name}}</span>
        {% if not loop.last and loop.length > 1 %}
          <span>, </span>
        {% endif %}
      {% endfor %}
    {% endapply %}
    </span> 
  </div>
{% endif %}