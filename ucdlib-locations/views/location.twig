{% extends getUcdTemplate('base') %}

{% block content_container %}
<div class="l-container l-basic--flipped">
  <div class="l-content">
    <ucdlib-hours
    api-host="{{site.site_url}}"
  ></ucdlib-hours>
  <p>{{foo}}</p>
  <div>
    {#
    <ucdlib-hours-today
      api-host="{{site.site_url}}"
      location="{{post.ID}}"
      show-children
    ></ucdlib-hours-today>
    #}
  </div>
  {{dump(post.core_data)}}
  {{post.content}}
  </div>
  <div class="l-sidebar-second">
    <p>hey girl, call me a sidenav cause i'm on your side</p>
  </div>
</div>

{% endblock %}

